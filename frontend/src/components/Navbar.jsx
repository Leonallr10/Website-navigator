function Navbar() {
  return (
    <header className="navbar">
      <div className="navbar-inner">
        <div>
          <h1 className="brand-title">Website Navigator</h1>
          <p className="brand-subtitle">Browse uploaded URLs without leaving the app.</p>
        </div>
      </div>
    </header>
  );
}

export default Navbar;
